{
  "name": "@patternlike/api",
  "version": "0.2.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "//build": "Dry-runs the SAME environment `deploy` ships. Without --env production this validated the top-level dev block (AUTH_STUB=1, issuer.invalid, the placeholder D1 id) and so could never catch a production-only config error; it also tripped Workers Builds' Worker-name check, whose remedy is a PR renaming the top-level worker to patternlike-api-production. Requires apps/web/dist to exist — see the root //build note.",
    "build": "wrangler deploy --dry-run --outdir=dist --env production",
    "//typecheck": "BOTH configs. Worker sources compile without Node types on purpose; scripts/ needs them. One command so a script cannot drift out of the gate.",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.scripts.json --noEmit",
    "//publisher:pattern:model:verify": "PRODUCTION GATE, not part of any test lane. Uses OPENAI_API_KEY to prove every distinct compiled Pattern model exists and accepts the contract's pattern keyword in a minimal strict-schema response. Prints no provider prose. Required before initial enablement and after any Pattern model or strict-schema derivation change.",
    "publisher:pattern:model:verify": "tsx scripts/verify-openai-pattern-model.ts",
    "ontology:corpus:prepare": "tsx scripts/prepare-ontology-corpus.ts",
    "//ontology:release:verify": "Offline second opinion on a downloaded active ontology bundle: recomputes the bundle hash from the canonical signing payload, verifies the signature against an explicitly supplied keyring, compiles the release, and requires machine_pipeline provenance. Every expectation is an explicit argument and the output is bounded to identity plus a closed pass/fail code -- release rules and reader-facing meanings never reach a terminal, a CI log, or a ticket.",
    "ontology:release:verify": "tsx scripts/verify-active-ontology-bundle.ts",
    "test:pattern-model-verify": "tsx --test scripts/verify-openai-pattern-model.test.ts",
    "test:ontology-corpus-prepare": "tsx --test scripts/prepare-ontology-corpus.test.ts",
    "test:ontology-release-verify": "tsx --test scripts/verify-active-ontology-bundle.test.ts",
    "test:wrangler-config": "tsx --test scripts/wrangler-config.test.ts",
    "//test": "Five lanes. The ordinary serial suite, the pre-0003 compatibility lane, then the operator-script lanes - the Pattern-model gate, the corpus builder, the offline ontology verifier, and the Wrangler resolution check. All four live outside both vitest include patterns, so chaining them here is what keeps them run at all.",
    "test": "vitest run && vitest run --config vitest.m3-compat.config.ts && npm run test:pattern-model-verify && npm run test:ontology-corpus-prepare && npm run test:ontology-release-verify && npm run test:wrangler-config",
    "db:local": "wrangler d1 migrations apply patternlike-ops --local",
    "//deploy": "Ships apps/web/dist as static assets. Prefer `npm run deploy:api` from the repository root, which builds the web app first.",
    "deploy": "wrangler deploy --env production",
    "//db:local": "Applies the ordered db/d1 directory, not just 0001. Deleting .wrangler state is an explicit developer reset, not the migration mechanism."
  },
  "//moment-timezone": "DATA ONLY. src/services/tzdb.ts imports data/packed/latest.json and decodes it; the library's own runtime is a UMD file inside a CommonJS package that @cloudflare/vitest-pool-workers refuses to load, and pulling in `moment` to answer two questions would put a date-formatting library in a Worker that needs neither. The dependency exists so package-lock.json pins the tz database version M5 day boundaries are resolved against — Intl exposes none.",
  "dependencies": {
    "@patternlike/pattern-engine": "0.7.0",
    "@patternlike/reading-engine": "0.3.0",
    "@patternlike/shared": "0.2.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "hono": "^4.12.32",
    "moment-timezone": "^0.6.3",
    "tz-lookup": "^6.1.25"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.1",
    "@cloudflare/workers-types": "^4.20250525.0",
    "@types/node": "^22.15.21",
    "@types/tz-lookup": "^6.1.2",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.16.1"
  }
}

# Roadmap integration results — 2026-09-11

The [approved sequence](../superpowers/plans/2026-09-11-roadmap-integration.md) has produced a reviewed integration branch from main `69a4f78`. [PR 54](https://github.com/henryperkins/patternlike-app/pull/54) carries the actual full-gate, migration, merge and deployment outcomes. This document records the implementation and local assessment; the dated September 9 records remain intact.

## Delivered implementation

- Reconciled the roadmap/specification decisions and restored source/runbook corrections plus optional map tools. Withdrawn Slice 2 stays withdrawn. The archived map snapshot remains historical/stale; no mandatory map gate was introduced.
- Recovered the separate unsigned quality candidates and safety 1.0.1 correction. Technical review reproduced the baseline/0.1.1/0.1.2 identities and all 22 retained prose artifact hashes. Old Pattern commands with a historical creation-source identity cancel before provider execution; no saved reading is rewritten and no candidate is activated.
- Integrated Today feedback beside independent check-in, categorized encrypted exact-edition events, explicit grant preconditions, idempotent replay, 24-month retention, export/erasure/key rotation and a paired-version compiler. Generation effects remain off. Current unsupported theme associations still produce no ranking signal. Candidate loading now examines at most the newest 100 matching rows before target/category/grant rejection; the engine's existing shared 20-feedback admission cap remains downstream. Older candidates can still fall outside that bounded window.
- Corrected initial Pattern failure presentation: Generate requires explicit `retryable: true`; false/missing eligibility retains read-only Check again. Existing regeneration presentation remains intact.
- Implemented process-local persistent sequential 4:1:1 runner slots with per-class error cooldowns. One operation remains in flight, existing lifecycle/budgets remain owned by the domain functions, and safe logs identify work class/policy. A controlled 240ms trace changed from 24 text/0 portrait/0 mesh completions to 8/2/2 under its stated job durations. This proves opportunities, not production latency, cross-restart fairness or isolation from a hung call. Installation/restart was not performed.

The original source-map checkout was preserved. The new branch retained all three ReadingConnections files from the P2 commit unchanged. The main checkout's original local documentation edits were backed up and compared with the later recorded decisions before reconciliation.

## AI editorial assessment

The user selected AI reviewers. Two independent agent contexts read all four available writer outputs before seeing one another's findings. The [assessment and reconciliation](artifacts/interpretation-quality/2026-09-11-ai-review/README.md) retain hashes, source bindings, claim coordinates, ordinal scores and disagreements. No definite hard defect or suppressed-feature leak was established; generic/repetitive prose and several uncertain elaborations remain concrete follow-ups. The historical exact-chain rejections and unknown-time helper acceptances were not changed. The assessment covers these four outputs, not all twelve chart cases or the whole corpus.

This is same-platform, source-aware AI review. It does not establish human comprehension, empirical psychological validity, measured reader benefit or corpus certification. Historical human worksheet fields remain unfilled.

## Rendered checks and AI comprehension walkthrough

Browser plugin was not available, so Playwright used an existing cached Chromium, temporary font/library configuration and local Vite servers. The tests use real application/components with fictional API data. No provider or real account was used. Temporary scripts, logs and screenshots are in `/tmp/patternlike-roadmap-browser/`; they are local evidence, not permanent hosted artifacts.

| Exercise | Observed result |
| --- | --- |
| Real App: Today response and independent check-in, then exact receipt in History | Passed at 1440×1000 and 390×844; category/notes payload and separate check-in asserted; no application errors, unexpected API routes or horizontal overflow; scoped History Axe A/AA check passed. |
| Existing correction flow and browser Back | Passed on both viewports; navigation alone caused no correction/consent mutation. |
| Real connected components: passage explanation → exact Pattern chapter → return → Timing → saved Daily | Passed on both viewports; support explanation displayed, exact content preserved and all 20 recorded requests per journey were GETs. |
| Saved-Daily feedback draft during foreground revalidation | Radio/note draft remained through the access check on both viewports; no feedback write occurred. |
| Observatory 3/4/5/6 chapters × two viewports × default/reduced motion | All sixteen combinations passed bound-chapter focus, long-text presence, injected WebGL context loss, retry/recovery, source-history preservation, return focus and complete-reading access. Artwork status was requested only for four-chapter documents. |

Inspected desktop and mobile screenshots show readable text, named chapter choices and the existing long-reading continuation affordance without horizontal overflow. Software-renderer GPU readback warnings and a temporary harness favicon 404 were observed separately from application errors. The early harness attempts required correct local font configuration and corrected test selectors; those harness failures did not require product changes. Physical devices, arbitrary browser engines and production authentication remain outside these checks.

The coordinating AI answered the five existing comprehension prompts from these rendered observations, source-bound fixtures and retained unknown-time material. These are AI explanations, with no participant success/difficulty score:

| Prompt | AI explanation and limit |
| --- | --- |
| Which edition is this? | The displayed date, History surface and matching receipt identify the saved edition; the connection binds reading/revision/hash rather than substituting Today. Later revisions show an r-number. Revision 1 is not explicitly labeled in the ordinary reading header, so an exact revision answer can require Connection details or fixture information. Making that distinction easier is a remaining presentation follow-up. |
| Why does the connection exist? | A retained common calculated feature, common natal participant or dated occurrence authorizes the particular edge. Similar wording alone does not. The tested navigation returns to its exact source; a shared date/participant is not proof of causation or interpretive truth. |
| Calculation versus interpretation? | Timing/aspect coordinates are calculated facts in the bound fixture; the reflective claims are interpretations grounded in the synthetic corpus. The connection's evidence receipt establishes retained support/identity, not psychological truth. The expanded details state that boundary, while standalone prose still needs clearer evidence-kind explanation. |
| What was withheld or unknown? | The retained unknown-time outputs withhold houses, angles and time-sensitive scope and explain narrower context. A missing connection can mean absent retained support or an unavailable destination; it does not establish an absence of a quality in the person. The AI prose review found no asserted suppressed feature. |
| What does feedback change? | Submission records an encrypted response for the exact edition and explicitly creates/reuses/renews permission. A seven-day possible effect is distinct from 24-month storage; unclear has no generation signal, relevance needs supported associations, and notes are excluded from provider context. Current effects are off; eligibility is not evidence of actual influence. Check-in has its separate freshness/storage rules. Opening birth correction changes neither details nor permission. |

## Verification and next work

Focused verification passed: 163 combined feedback/journey web tests; 179 affected feedback/API tests; 119 safety/proof tests; 7 builder tests; 167 runner tests and build; 80 API claim/lease tests; and 69 optional map-tool tests. These sets overlap and are not an additive total. Independent production review found no actionable defect.

The first complete local gate passed thirteen lanes and caught an asynchronous feedback-loading expectation in the web accessibility test; the test now awaits the settled feedback heading without changing the options/error fixture or Axe assertion. The full web suite then passed all 809 tests. The final existing `ci:local` summary belongs in PR 54 before merge; no focused test claim substitutes for it. API verification emitted a Cloudflare/Vitest teardown warning despite passing its tests, recorded in the logs rather than presented as a product incident.

A September 11 read of production metadata confirmed the earlier P2 commit `69a4f78` was serving with `auth_stub: false`, Worker version `5dce8886-71e7-4e8b-adec-036d7707ac88`. The migration ledger still ended at 0030 before this integration release. The new Worker references 0031 storage through routes, privacy and export even with effects off, so 0031 must precede compatible deployment. The PR records subsequent operations. Ontology signing/activation, compiler enablement and installed-runner adoption remain separate.

Next prioritize the broader Slice 7 composed readiness and safe operational metrics, using the completed retry/fairness behavior as the foundation. For prose, narrow the disputed mechanisms, name available supported features and reduce repetitive paraphrases before claiming stronger specificity. Keep real-reader outcome measurement explicit if pursued. Wider generated artwork, generic refactors, vocabulary/calculation naming and machine-ontology re-entry remain conditional rather than dependencies for this journey.

The actual PatternExperience account component passed the final Chromium browser check at 1440, 390, and 320 pixels. The final run used ordinary pointer clicks for graphics recovery in both normal and expanded scenes; it did not use force clicks or the earlier keyboard workaround. All checks passed with zero console errors and warnings.

This is a local fictional account proof with intercepted API responses, not live account delivery. The test starts with an existing written Pattern and automation off. It changes only the automatic-portrait preference; controlled status responses advance from pending to ready. The four accepted generated programs are compiled unchanged with the combined document identity.

[report.json](report.json) records request counts, per-width checks, blob cleanup, model/source hashes, the complete download hash and size, and exact JSON payload equality. The base64 download itself is omitted to avoid duplicating its images and models. [account-proof.js](account-proof.js) is the exact final Playwright CLI script. [fixture.mts](fixture.mts) is the fixture generator; its local paths identify the four original canary inputs. The two harness files show the real component import and the temporary cleanup hooks. These files are audit evidence and retain the paths used for this run.

The local reproduction command, after generating the four accepted canary artifacts and starting the copied Vite harness on port 4187, was:

```bash
source /home/henry/.nvm/nvm.sh
nvm use 22
npx tsx /tmp/portrait-automation-account/fixture.mts
/home/henry/.codex/skills/playwright/scripts/playwright_cli.sh --session portrait-automation run-code --filename /tmp/portrait-automation-account/account-proof.js
```

The original screenshots cover desktop whole portrait, comparison and full reading; phone whole portraits and graphics fallback; and expanded graphics fallback at all three widths. Source paragraphs remain complete during pending generation and in the full reading. The final run also checks all four chapters, facets, keyboard navigation, dialog focus, guide progress, asset reuse, and revocation of all created private/download blob URLs.

### Result
{"unknownTimeSupportedPlacements":1,"moonAndRisingUnavailable":true,"unknownTimeHouseAbsent":true,"approximateRisingQualified":true}
### Ran Playwright code
```js
await (async page => { await page.getByRole("combobox", {name:"Example birth-time accuracy"}).selectOption("unknown"); await page.getByRole("button", {name:"Your sky",exact:true}).click(); const choices = page.getByRole("group",{name:"Birth-chart placements"}); const labels = await choices.getByRole("button").allTextContents(); if (labels.length !== 1 || !labels[0].includes("Sun")) throw new Error("Unsupported unknown-time placement"); const unavailable = await choices.innerText(); if ((unavailable.match(/Birth time unknown/g) ?? []).length !== 2) throw new Error("Missing unavailable qualifications"); const reading = await page.getByRole("complementary",{name:"Birth-chart reading"}).innerText(); if (reading.includes("House ")) throw new Error("Unknown-time house disclosed"); await page.getByRole("complementary",{name:"Birth-chart reading"}).scrollIntoViewIfNeeded(); await page.screenshot({path:"output/playwright/merge-sequence/built-unknown-time.png",scale:"css"}); await page.getByRole("combobox",{name:"Example birth-time accuracy"}).selectOption("approximate"); await page.getByRole("button",{name:"Your sky",exact:true}).click(); await page.getByRole("button",{name:"Rising in Libra",exact:true}).click(); const qualified = await page.getByRole("complementary",{name:"Birth-chart reading"}).innerText(); if (!qualified.includes("Birth time is approximate.")) throw new Error("Approximate time unqualified"); return {unknownTimeSupportedPlacements:labels.length,moonAndRisingUnavailable:true,unknownTimeHouseAbsent:true,approximateRisingQualified:true}; })(page);
```

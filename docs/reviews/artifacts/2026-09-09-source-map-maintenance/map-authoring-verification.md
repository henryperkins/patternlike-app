# Task 3: maintained map and archives

Status: owned authored files frozen for root integration. No commits, pushes, merges, provider calls, signing, activation, or runtime changes. All writes used the explicit isolated checkout `/home/henry/patternlike-app-source-map`; the original checkout was not edited.

## Deliverables

- `docs/architecture/source-map/map.json`: 8 branches, 42 topics, 97 leaves, 199 evidence entries, 117 distinct referenced files. The eight corrected-map branch subjects are retained. Counts are derived, not a target. There is one recorded-observation leaf; all other evidence is current source.
- `docs/architecture/source-map/README.md`: maintenance, check interpretation, refresh, source identity, archive limitations and exact hashes. Publication is explicitly pending root capture; root must replace the planned name paragraph with the selected reviewed snapshot links after capture/check.
- Five byte-identical archived originals under the two specified generations. Originals and roadmap untouched.

## Source meaning review and corrections

The corrected 38-topic, 83-claim, 129-anchor overview was the starting point. I inspected referenced source contexts and owning implementation boundaries, replacing import-only and incidental anchors and correcting inherited claims. The useful current Observatory implementation is present in this isolated checkout; its three-to-six chapter stations, reading folios, default entry, controls and graphics cleanup are retained in the map.

ST-08: split Offline ontology supply from Parked machine-ontology producer; explain unsigned compilation, internal compatibility fields, both omitted fragment groups, signature-only signing return, origin/public admission, and remaining eligibility gates. Added one explicitly dated September 6 active-pointer-join observation without claiming a current signature or complete evidence census. Added Shared Codex exchange mechanics and literal committed Observability and release identity. Daily V1 command dispatch retains deterministic V3 execution; V2 uses V5, with Today explicitly choosing V5. Selection and plan/candidate validation cite owning engine functions and actual executor call sites.

ST-09: health/meta outside configuration guard, separately guarded session exchange, deletion-status guard plus dedicated receipt cookie, and distinct product/service/admin/runner/crypto-operator authorities. Removed the blanket configGuard and unauthenticated deletion-status claims.

ST-10: native HTTP operational canary with optional authenticated POST place search that can exercise geocoding; Codex runs for fresh Daily/Pattern/verifier evaluations; Daily and verifier preparation modes; local gate capture/verify separate from supplied-record offline reconciliation. Presence of tooling is never an execution receipt.

ST-11: revision reason describes purpose, not actor or scheduler provenance. Fact repair chooses defect_repair, scheduler reserves it, retry reconstruction preserves the reserved reason. The internal reissue route remains cited.

ST-12: explicit chart-bound Privacy automation control; publication outbox and automation/runner coordination; withdrawal cancels unfinished work without rewriting the reading or asserting erasure of all completed assets. Four-chapter artwork eligibility is distinct from three-to-six chapter reading stations.

Other evidence improvements: Today/History switch call sites, birth calculation budget/client, feedback compiler and write route instead of stub prose, signing/content graph/hash validators, production-specific asset/storage/model settings, actual crypto functions, and source metadata for provenance gaps. Gate/account/branch claims are attributed to AGENTS guidance rather than presented as fresh GitHub observations. Removed a scratch-candidate delivery claim whose inherited build-test import anchor did not establish that boundary. These additions and precision fixes were communicated to the source correction agent, who owns ST-01–ST-12 dispositions.

## Verification performed

- Read binding task brief and specification; inspected corrected plain/anchored map, source contexts, source symbols and actual call sites with `rg`, `sed`, `cat` and bounded Python read scripts.
- Archived before adaptation with exact byte copies and SHA-256; checked originals versus archives again after authoring.
- Node `v22.23.2`: called the implemented `validateModel` over maintained `map.json`. Result: valid; counts 8/42/97/199/117.
- Independently checked all 199 literal selectors for exactly one case-sensitive contiguous match after CRLF-to-LF normalization. Checked each target is regular UTF-8, is not a symlink or under a symlink component, is not ignored by Git, is outside output and the maintained map directory, and selector length is 1–2048 UTF-8 bytes. All passed.
- Checked the committed fragment data: 60 total, twelve section 2 fragments, eight section 8 fragments, leaving 40 emitted by the inspected predicate mapping. The root/source-correction unit owns the separately executed real offline builder/hash receipt; this report does not conflate data counting with that run.
- `git diff --check -- docs/architecture/source-map`: exit 0. The maintained files are new/untracked at this stage; the explicit model and byte checks above cover them independently.
- Root owns final map capture/check, renderer regression tests, integrated serial content test, full local merge gate, and publication links. No success claims for those pending commands are made here.

All evidence IDs are globally distinct from branch/topic/leaf IDs; every entry is used and every leaf cites one evidence class. Titles/prose are single-line plain text. Captured complete source identities must come from root's fresh bundle, not this report or the historical manifests.

## Archive before-copy identities

| Original path | Bytes | SHA-256 |
| --- | ---: | --- |
| `output/mindmaps/2026-09-07-184743/patternlike-source-mindmap.md` | 11778 | `a6f49b58777a4e43e29fd13778f33c03f50dec23b7cbc1dcccf2fa7f666ca2e3` |
| `output/mindmaps/2026-09-07-184743/source-evidence.md` | 17032 | `bd8babe923cc9630fd61b41d52b76f06e497d0cdc5027d24fc990d3e4e3cec00` |
| `output/mindmaps/2026-09-07-184743/source-snapshot.json` | 21695 | `a54b6367b014389c5036584950feb6455f2b8697d5f4af0fbfdbda306d71ec3a` |
| `output/mindmaps/2026-09-09-corrected/patternlike-source-mindmap.md` | 15585 | `b09bc6e9275e5e530423f76ebbbc1b80447bb9e71356bf39214c6d91f9d74e9e` |
| `output/mindmaps/2026-09-09-corrected/patternlike-source-mindmap.with-anchors.md` | 22084 | `05fca0beb6314ce2d0889f4a229c1d48221bde58de0e4965c895355fd8c3606a` |

All five archive copies and their originals still matched these values at final verification. No original capture identity was invented for the corrected September 9 generation.

Authored map SHA-256 at handoff: `f026739da988e2d8b88de87de7e5d450f12b3815e6580c921958d51663027dfe`.

## Integration review refinement

Added direct owning citations for the existing shared-package licensing-boundary leaf: the still-required decision item in `docs/legal/SWISS_EPHEMERIS_LICENSE_DECISION.md` section 6 and the unresolved-boundary explanation in `LICENSING.md`. The claim and legal position remain unchanged.

README now explicitly requires Node 22 on Linux with functioning `/proc/self/fd` directory-handle paths for capture/check. Unsupported environments refuse operations with `io_failed`, exit 2. This does not restrict archived content or reading/sharing generated Markdown. Root still owns final publication links.

Final counts after refinement: 8 branches, 42 topics, 97 leaves, 201 evidence entries, 119 referenced files. Earlier 199/117 counts above describe the prior handoff.

Updated authored map SHA-256: `79a894097c333d1b6fad2cd20526c68a553c92769af5f2f76fb9ff72a5947847`.

Fresh refinement checks passed under Node v22.23.2: implemented validateModel returned 8/42/97/201/119; independent checks resolved all 201 selectors exactly once across 119 safe regular UTF-8 nonignored nonsymlink files; all five archives and originals retained their before-copy identities; scoped git diff --check exited 0. Owned map/README/report files are frozen again for root integration.

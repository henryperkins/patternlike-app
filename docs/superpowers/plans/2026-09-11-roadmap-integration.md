# Roadmap integration — 2026-09-11

Authorization: “Proceed with recommended sequence,” following the September 11 progress review. This is the bounded integration of existing Slices 1/3/5 work and the concrete retry/fairness follow-ups, using the existing slice specifications. It does not revive withdrawn Slice 2 or activate ontology candidates or categorical generation effects.

1. Start from main `69a4f78`, preserve the P2 feedback-draft fix, and recover outstanding source-map work in focused commits. Reconcile the later recorded decisions with the main checkout's existing documentation edits; preserve historical records.
2. Review and integrate the offline quality builder, retained safety correction and evidence. Preserve candidate identities and old command semantics; do not sign or activate a candidate.
3. Review and integrate the categorical feedback interface, encrypted lifecycle, additive contracts/migration and default-off compiler. Correct the pre-eligibility candidate cap with a bounded scan and a real integration regression.
4. Correct initial Pattern failure actions using the API retryable flag, with regression coverage for false/missing flags and preservation of regeneration behavior.
5. Complete the rendered observatory 3/4/5/6 chapter, long-text, reduced-motion and graphics-recovery checks. Use the user-selected independent AI primary/secondary editorial reviews and simulated comprehension tasks. Keep actual human comprehension and authenticated production observations distinct and open when unavailable.
6. Implement and verify persistent sequential 4:1:1 runner rotation using the existing claim/execute/complete functions and six-slot opportunity bound. Broader composed readiness and operator metrics remain a separate next implementation; no concurrency increase or ontology enablement.
7. Review the combined branch, run the existing full local merge gate, record its actual summary in the PR, and release compatible feedback only with migration 0031 applied first. Preserve default-off effects. Observe actual deployment separately from local verification.

Verification uses existing commands and focused regressions, with no new gate tooling. The original main and source-map checkouts are preserved; integration uses `codex/roadmap-integration` in `.worktrees/roadmap-integration`.

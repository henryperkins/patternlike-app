# Reading assurance corrections

The September 6 review and local reproductions show that Daily accepts incorrect fact relationships and that the authored ontology route omits the machine release regression checks. The user approved implementation with “Proceed” after the verification report and remediation sequence.

Daily will keep the existing public and provider JSON shapes. Internal constrained facts will retain discriminated, role-preserving support data projected directly from frozen calculation inputs. The candidate validator will require each explicit factual sentence to be supported by a single cited record, including subject/target roles, placements, degree kind, event timing, ingress direction, and collective/personal scope. A factual sentence must match the entire bounded grammar; reflection and separate relationships use separate sentences. UTC normalization and numeric half-open day bounds prevent offset or precision errors. Cycle orb values describe configured envelope limits, not measured separations. Unsupported or ambiguous factual language fails closed. This validates the actual emitted prose; it does not trust an extra model-authored claim ledger. Headlines cannot contain uncited factual assertions. Required uncertainty disclosures remain possible without asserting suppressed calculations. Reflective interpretation remains generated language and is not claimed to be scientifically established or semantically entailed by calculated positions.

The selection and validation policy versions and Daily prompt pin will advance where their behavior changes. Existing stored readings retain their historical provenance. Frozen old commands fail closed through the existing unsupported-policy path; they are not silently reinterpreted under new rules. Contracts retain their wire shapes, with explanatory freeze notes for changed policy semantics.

Pattern will apply reusable deterministic publication safety checks for both ontology origins over the bound writer, packet, selection manifest, ontology, semantic verdict, and public projection. Checks cover suppressed calculations, evidence and source dependencies, prohibited claims, mandatory features, private fields/identifiers, and semantic refusal. Rejection uses existing bounded retry/terminal state transitions and preserves a prior readable Pattern during failed replacement. The check result must be bound to candidate identity and policy version. The release harness will use shared checks without weakening its existing gates. A passing release corpus and a passing document are distinct evidence.

Corpus provenance will explicitly record unknown historical generation fields and incomplete human certification. No human sign-off, account privacy setting, or live deployment observation will be invented. Repository-side release evidence will bind source snapshots, test receipts, build/artifact hashes, model/prompt/policy pins, migrations, and recorded versus observed deployment state, reusing existing work where possible without modifying unrelated worktrees. Production changes and a test-account lifecycle exercise remain separate from this local implementation.

Verification requires regressions that first fail for the observed defects, focused affected suites, review of the final changes, and all 14 `ci:local` lanes against the final frozen source tree. Node follows `.nvmrc` (22); Python uses a real per-worktree `.venv`. The current working branch starts at `50f452a`, retaining reading history and revision-specific saves.

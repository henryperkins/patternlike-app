# Review claim follow-up

Approved direction: address the remaining claims in the September 6 review after
the first local reading-assurance pass. Continue in the same isolated worktree.

## Contracts

1. Public copy distinguishes calculated chart facts from generated interpretation.
   Consent describes multiple bounded provider requests, application retention,
   and account-dependent upstream treatment without promising unverified non-use.
2. Text generation uses the existing isolated Codex JSON transport: pinned CLI,
   ChatGPT authentication, fresh ephemeral thread, no model tools, host instruction
   isolation, verified effective settings, bounded output and fatal cleanup failure.
   Preserve frozen models, prompts and response schemas. Do not change accounts.
3. A dedicated reversible Pattern generation switch stops new work and prevents
   unfinished work from publication while disabled. Accepted reading access,
   consent withdrawal, deletion and cleanup continue. Resume only eligible work;
   deletion, withdrawal and obsolete source state never become eligible again.
4. Image provenance distinguishes the configured/requested image model from
   provider-observed identity. The latter remains unknown when not returned.
   Existing saved assets stay readable and old receipts never acquire invented
   provider attestations. Version or compatibly extend non-frozen protocols.
5. Fresh-output evaluation tooling uses actual runner prompts and validators,
   records source/model/prompt identity, preserves rejected cases and distinguishes
   automatic checks from independent editorial assessment. Synthetic replay stays
   labelled synthetic. No output sample is called a production success rate.
6. Corpus review tooling binds named reviews to exact fragment hashes and explicit
   criteria. It never fills historical provenance from current runtime settings,
   signs as a human, or treats model review as independent human certification.
7. Release evidence can reconcile a local gate with separately supplied deployed
   Worker/runner/migration observations and authorized account lifecycle evidence.
   Partial evidence stays partial; declarations do not become live observations.
   Preserve the unrelated release-truth worktree and avoid duplicating its runtime
   migration without a separate reviewed integration.

## Boundaries and verification

Local implementation, focused regressions, browser checks for copy, and the final
14-lane aggregate gate are in scope. Preserve all first-pass fixes. No commit,
push, production migration or deployment is part of this local implementation.
Account-specific settings, attributable human editorial certification, historical
corpus generation provenance and deployed lifecycle evidence require real records;
tooling and explicit open status can prepare but cannot manufacture them.

Missing chat, social features, native applications, historical versions and broad
atlas parity are strategic product questions rather than new feature requirements.
An issuer migration must preserve account identity and crypto subject mappings;
the tenant name alone does not require a migration.

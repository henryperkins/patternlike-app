{
  "name": "@patternlike/reading-engine",
  "version": "0.3.0",
  "private": true,
  "//license": "UNLICENSED. Deliberately NOT @patternlike/shared: apps/calc-stub is AGPL-3.0-or-later and imports shared, so Corresponding Source for that service reaches into whatever it imports. Ranking weights, eligibility rules, and editorial assembly are the product and must not land inside a package the AGPL service pulls in. See LICENSING.md.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "//test": "Glob QUOTED so node expands it rather than sh — see the note in packages/shared/package.json.",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "generate:allowlist": "python scripts/generate_source_allowlist.py"
  },
  "//dependencies": "TYPE-ONLY. @patternlike/shared owns the M5 wire types this package compiles against; importing them keeps one declaration of ReadingGenerationRequest rather than a structurally identical copy that drifts. The direction is safe for LICENSING.md: apps/calc-stub imports shared, shared does not import this package, and ranking, eligibility, selection, prompt, and validation policy all stay here.",
  "dependencies": {
    "@patternlike/shared": "0.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
